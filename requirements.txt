# VisionTrack — Requirements
# Install: pip install -r requirements.txt

# ── Core Detection ─────────────────────────────────────────────────────────
ultralytics>=8.4.0          # YOLOv8 (modern public API)
torch>=2.0.0                # PyTorch (CPU; for GPU: see pytorch.org)
torchvision>=0.15.0

# ── OpenCV ─────────────────────────────────────────────────────────────────
opencv-python>=4.8.0

# ── Numerical ──────────────────────────────────────────────────────────────
numpy>=1.23.0
scipy>=1.9.0

# ── Tracking algorithms ────────────────────────────────────────────────────
lap>=0.5.0                  # Linear assignment (ByteTrack, OC-SORT)
filterpy>=1.4.5             # Kalman filter (OC-SORT)
easydict                    # Config dict (StrongSORT)

# ── Backend ────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6     # File uploads
websockets>=12.0
aiofiles>=23.0

# ── Logging / utilities ────────────────────────────────────────────────────
loguru>=0.7.0
PyYAML>=6.0
psutil>=5.9.0               # System resource monitoring

# ── ZMQ (existing sender module) ───────────────────────────────────────────
pyzmq

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0               # TestClient for FastAPI

# ── ReID models (optional, needed for StrongSORT/BoT-SORT/DeepOCSORT) ──────
# gdown                     # Download ReID weights from Google Drive
# torchreid                 # Deep person ReID models